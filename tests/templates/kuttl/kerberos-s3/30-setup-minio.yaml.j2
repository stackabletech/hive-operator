---
# NOTE: nothing is actually read/written to Minio/S3 in this test case.
# It is still required to have a Minio instance because, for tables with an S3 location, HMS performs a check
# to ensure that the configured bucket/path actually exist.
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: >-
      helm install minio
      --namespace "$NAMESPACE"
      --version 12.6.4
      -f helm-bitnami-minio-values.yaml
      --repo https://charts.bitnami.com/bitnami minio
    timeout: 240

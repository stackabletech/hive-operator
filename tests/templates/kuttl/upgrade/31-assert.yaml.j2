---
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 600
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: hive-metastore-default
  labels:
{% if test_scenario['values']['hive-old'].find(",") > 0 %}
    # Yes, this *might* not work with custom images, I'm sorry!
    app.kubernetes.io/version: "{{ test_scenario['values']['hive-new'].split(',')[0] }}-stackable25.3.0"
{% else %}
    app.kubernetes.io/version: "{{ test_scenario['values']['hive-new'] }}-stackable25.3.0"
{% endif %}
status:
  readyReplicas: 1
  replicas: 1
